import {connect} from "react-redux"
import UnfeaturedPopularStory from "./unfeatured_popular_story"

const mapStateToProps = (state) => {
    // return({
    //     story: 
    // })
}

export default connect(mapStateToProps)(UnfeaturedPopularStory)